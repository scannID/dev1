package com.scanny.config;

import org.springframework.context.annotation.Configuration;

/**
 * Intentionally empty — CORS is configured solely in {@link SecurityConfig}
 * to avoid conflicting policies.
 */
@Configuration
public class WebConfig {
}
